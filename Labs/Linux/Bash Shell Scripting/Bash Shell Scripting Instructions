**BASH SCRIPT SHELL**

**USING SSH TO CONNECT TO LINUX**

1 - I select the **Download PPK** button and save the **_labsuser.ppk_** file.

2 - I made a note of the **Public IP address**.

3 - I downloaded **PuTTY** so I can SSH into the Amazon EC2 instance.

4 - I configured my PuTTY session by following the directions provided in the guide to connect to my Linux instance using PuTTY. Top of FormBottom of Form

**TASK 1: CREATE AN ALIAS FOR A BACKUP OPERATION**

I need to create an alias that gives me the ability to back up whatever path I provide.

1 - To validate that I am in the home folder in the terminal, I entered the **_pwd_** command and pressed Enter.

2 - To create an alias called backup, I entered the following command: **_alias backup='tar -cvzf '_** and pressed Enter.

3 - To use the backup alias to back up the CompanyA folder, I entered the following command: **_backup backup_companyA.tar.gz CompanyA_** and pressed Enter.

4 - To verify that the archive is created, I entered the **_ls_** command and press Enter.

**TASK 2: EXPLORE AND UPDATE THE PATH ENVIRONMENT VARIABLE**

5 - In this task, I display the PATH environment variable. I then update the variable and add a new directory in which I can place executables.

6 - To navigate to the bin folder in the home CompanyA directory, I entered the following command: **_cd /home/ec2-user/CompanyA/bin_** and press Enter.

7 - To run the **_hello.sh script_**, I entered the following command: **_./hello.sh_** and press Enter.

8 - To navigate to the parent folder, I entered the following command: **_cd .._** and press Enter.

9 - To run the hello.sh script again, I entered the following command: **_./bin/hello.sh_** and press Enter.

10 - To run the **_hello.sh_** script, I entered the following command: **_hello.sh_** and press enter.

- **Expected Output**:
- \[ec2-user@ CompanyA\]\$ hello.sh
- bash: hello.sh: command not found

11 - I analyzed the three different ways I tried to run the **hello.sh script**. In the next step, I will figure out why the third run failed and how to solve this issue.

12 - To display the value of the PATH variable, I entered the following command***: echo \$PATH*** and press Enter.

13 - I navigate to the **/home/ec2-user/CompanyA/**bin folder and entered **hello.sh** to run the script.

14 - I can also enter **/home/ec2-user/CompanyA/bin/hello.sh** from any folder.

15 - To add the **/home/ec2-user/CompanyA/bin** folder to the PATH variable, I entered the following command: **_PATH=\$PATH:/home/ec2-user/CompanyA/bin_** and press Enter.

16 - To try running the **hello.sh** script again, I entered **_hello.sh_** and press Enter.
